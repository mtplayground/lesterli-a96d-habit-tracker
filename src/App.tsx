import './App.css'

function App() {
  return (
    <main className="app-shell">
      <section className="intro" aria-labelledby="app-title">
        <p className="eyebrow">Habit Tracker</p>
        <h1 id="app-title">Build steady routines, one check-in at a time.</h1>
        <p className="lede">
          This Vite, React, and TypeScript foundation is ready for the habit
          tracking features planned in the upcoming issues.
        </p>
      </section>
    </main>
  )
}

export default App
